#include<bits/stdc++.h>

using namespace std;

using ll = long long;

int main(){
	cin.tie(0);
	ios::sync_with_stdio(false);

	int n;
	ll m;
	cin>>n>>m;
	vector<int> a(n);
	for(auto &b : a) cin>>b;
	sort(a.begin(),a.end());

	return 0;
}
